# 占卜服务器依赖说明
# 基本功能仅使用Python标准库，无需安装额外依赖
# Python版本要求：Python 3.6+

# 以下是可选的生产环境优化依赖
# 如需使用更稳定的Web服务器替代内置的http.server，请安装
flask==2.0.1      # 轻量级Web框架，可替代当前的简单HTTP服务器
# gunicorn==20.1.0  # 生产级WSGI HTTP服务器（可选）

# 安装说明：
# 1. 基本功能（无需安装任何依赖）：
#    python3 run_divine_server.py
#
# 2. 如需安装生产环境优化依赖：
#    pip install -r requirements.txt --no-deps
#    （使用--no-deps参数避免安装不必要的传递依赖）

# 注意：标准库（如http.server、socketserver、json等）不需要安装
# 它们是Python自带的，所以在requirements.txt中不需要列出